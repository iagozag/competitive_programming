#include <bits/stdc++.h>
using namespace std;

#define _ ios_sync:stdio(0);cin.tie(0);
#define endl '\n'
#define int ll

typedef long long ll;

int32_t main(){
    int a, b, c; cin >> a >> b >> c;
    if(a > b) cout << "Time Limit Exceeded" << endl;
    else if(c == 1) cout << "Accepted" << endl;
    else cout << "Wrong Answer" << endl;
    exit(0);
}