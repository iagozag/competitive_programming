#include <bits/stdc++.h>

using namespace std;

#define _ ios_base::sync_with_stdio(0); cin.tie(0);
#define endl '\n'

typedef long long ll;
#define int ll

int32_t main(){ _
	int w, a, b, c; cin >> w >> a >> b >> c;
	cout << (w >= a+b+c ? 'S' : 'N') << endl;
	exit(0);
};
