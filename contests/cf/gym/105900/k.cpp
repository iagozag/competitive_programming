#include <bits/stdc++.h>
using namespace std;

#define _ ios_base::sync_with_stdio(0); cin.tie(0);
#define endl '\n'

typedef long long ll;
#define int ll

int32_t main(){ _
    string s; cin >> s >> s >> s;
    if(s[0] == 'T') cout << "Staraptor, eu escolho voce!" << endl;
    if(s[0] == 'L') cout << "Torterra, eu escolho voce!" << endl;
    if(s[0] == 'S') cout << "Luxray, eu escolho voce!" << endl;
}