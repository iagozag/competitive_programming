#include <bits/stdc++.h>
using namespace std;

#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define endl '\n'

#define int ll

typedef long long ll;

int32_t main(){ _
	int a, b, c; cin >> a >> b >> c;
	cout << (a*b == c ? 'S' : 'N') << endl;
	
	exit(0);
}
